# HackRadar — local secrets (NEVER commit this file to git)
# Copy this file and fill in your values, then run:
#   .\.venv\Scripts\python.exe -c "from dotenv import load_dotenv; load_dotenv()" 
# Or just set these as $env: vars in PowerShell before running.

GEMINI_API_KEY=AIza_PASTE_YOUR_KEY_HERE

TELEGRAM_BOT_TOKEN=PASTE_YOUR_BOT_TOKEN
TELEGRAM_CHAT_ID=PASTE_YOUR_CHAT_ID

# WhatsApp / Twilio (optional — only needed if using WhatsApp)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
